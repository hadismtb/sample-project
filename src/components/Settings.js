import React from 'react';

const Settings = () => {
    return (
        <div className='settings'>
            <nav className="settingsNav">
                
            </nav>
        </div>
    );
};

export default Settings;